# 3.4 Bisection

## From book
x = float(input('Enter a negtive integer: '))
epsilon = 0.001
numGuesses = 0
low = x
high = 0
ans = (high + low)/2.0
while abs(ans**3 - x) >= epsilon:
    print ('low =', low, 'high =', high, 'ans =', ans)
    numGuesses += 1
    if ans**3 > x:
        high = ans
    else:
        low = ans
    ans = (high + low)/2.0
print ('numGuesses =', numGuesses)
print (ans, 'is close to cube root of', x)

## From MIT works              
cube = 27
epsilon = 0.01
num_guesses = 0
low = 0
high = cube
guess = (high + low)/2.0
while abs(guess**3 - cube) >= epsilon:
    print ('low =', low, 'high =', high, 'ans =', ans)
    num_guesses += 1
    if guess**3 < cube :
        low = guess
    else:
        high = guess
    guess = (high + low)/2.0
print ('num_guesses =', num_guesses)
print (guess, 'is close to the cube root of', cube)
